import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sixappeal.cricket',
  appName: 'Six Appeal Cricket',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
